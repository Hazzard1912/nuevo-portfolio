---
import Layout from "../../layouts/Layout.astro";
import Image from "astro/components/Image.astro";

const blogPost = {
    title: "Astro + React: La Combinación Perfecta",
    description:
        "Aprende a crear sitios web ultra rápidos combinando la potencia de Astro con la flexibilidad de React. Tutorial paso a paso.",
    publishDate: "2024-01-01",
    author: "Equipo de EstusCode",
    readTime: "8 min",
    image: "https://images.unsplash.com/photo-1627398242454-45a1465c2479?auto=format&fit=crop&q=80",
    sections: [
        {
            title: "¿Por qué Astro + React?",
            content:
                "La combinación de Astro con React ofrece lo mejor de ambos mundos: el rendimiento excepcional de Astro y la interactividad rica de React. Descubre cómo esta sinergia puede revolucionar tus proyectos web.",
            keyPoints: [
                "Rendimiento Optimizado",
                "Hidratación Parcial",
                "SEO Mejorado",
            ],
        },
        {
            title: "Configuración del Proyecto",
            content:
                "Aprende a configurar un proyecto desde cero integrando React en Astro, siguiendo las mejores prácticas y optimizaciones recomendadas.",
            steps: [
                "Instalación de Astro",
                "Integración de React",
                "Configuración de TypeScript",
            ],
        },
        {
            title: "Componentes Híbridos",
            content:
                "Explora cómo crear componentes que aprovechen las fortalezas de ambos frameworks, maximizando el rendimiento y la experiencia del usuario.",
        },
    ],
    codeExamples: [
        {
            title: "Integración Básica",
            description: "Cómo integrar componentes React en páginas Astro",
            code: `
---
// src/pages/index.astro
import ReactComponent from '../components/ReactComponent';
---

<div>
    <ReactComponent client:load />
</div>`,
        },
        {
            title: "Hidratación Selectiva",
            description:
                "Optimización del rendimiento mediante hidratación controlada",
            code: `
---
// Diferentes estrategias de hidratación
<ReactComponent client:load />    // Hidratación inmediata
<ReactComponent client:visible /> // Hidratación al ser visible
<ReactComponent client:idle />    // Hidratación en tiempo de inactividad
---`,
        },
        {
            title: "Paso de Props",
            description: "Comunicación entre componentes Astro y React",
            code: `
---
// Paso de datos desde Astro a React
<ReactComponent 
    data={fetchedData}
    client:load
/>
---`,
        },
    ],
    benefits: [
        {
            title: "Rendimiento Excepcional",
            description:
                "Sitios web que cargan en milisegundos gracias a la generación estática de Astro",
        },
        {
            title: "Flexibilidad Total",
            description:
                "Usa React donde necesites interactividad, mantén el resto estático",
        },
        {
            title: "SEO Optimizado",
            description:
                "Contenido generado en el servidor para mejor indexación",
        },
    ],
};

const title = "EstusCode | " + blogPost.title;
const metaTitle = "EstusCode | " + blogPost.title;
const description = blogPost.description;
const url = "https://www.estuscode.com/blog/astro-react-desarrollo";
const canonical = "https://www.estuscode.com/blog/astro-react-desarrollo";
const keywords = [
    "Astro",
    "React",
    "Desarrollo Web",
    "JavaScript",
    "TypeScript",
    "Rendimiento Web",
    "SSG",
    "Hidratación Parcial",
    "Frontend",
    "Optimización Web",
];
---

<Layout
    title={title}
    metaTitle={metaTitle}
    description={description}
    url={url}
    canonical={canonical}
    keywords={keywords}
>
    <article class="bg-white">
        <!-- Hero Section -->
        <div class="py-24">
            <Image
                src={blogPost.image}
                width={800}
                height={400}
                loading="eager"
                decoding="async"
                alt="Astro y React: Desarrollo web moderno"
                class="rounded-xl object-cover mx-auto"
            />
        </div>

        <!-- Content -->
        <main class="container mx-auto px-4">
            <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="prose prose-lg max-w-none">
                    <!-- Introducción -->
                    <p
                        class="lead text-xl md:text-2xl text-gray-600 mb-16 mt-8"
                    >
                        El desarrollo web moderno exige velocidad y
                        flexibilidad. Descubre cómo la combinación de Astro y
                        React te permite crear sitios web ultra rápidos sin
                        sacrificar la experiencia del usuario.
                    </p>

                    <!-- Secciones Principales -->
                    {
                        blogPost.sections.map((section, index) => (
                            <section
                                class="mb-20"
                                data-aos="fade-up"
                                data-aos-delay={index * 100}
                            >
                                <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-8">
                                    {section.title}
                                </h2>
                                <p class="text-gray-600 mb-8 text-lg leading-relaxed">
                                    {section.content}
                                </p>
                                {section.keyPoints && (
                                    <ul class="grid sm:grid-cols-3 gap-6 mt-6">
                                        {section.keyPoints.map((point) => (
                                            <li
                                                class="bg-gray-50 p-6 rounded-xl text-gray-700 
                                            shadow-sm hover:shadow-md transition-all duration-300"
                                            >
                                                {point}
                                            </li>
                                        ))}
                                    </ul>
                                )}
                                {section.steps && (
                                    <div class="space-y-4">
                                        {section.steps.map((step, i) => (
                                            <div class="flex items-center space-x-4">
                                                <span
                                                    class="flex-shrink-0 w-8 h-8 rounded-full bg-blue-100 
                                                text-blue-600 flex items-center justify-center font-semibold"
                                                >
                                                    {i + 1}
                                                </span>
                                                <span class="text-gray-700">
                                                    {step}
                                                </span>
                                            </div>
                                        ))}
                                    </div>
                                )}
                            </section>
                        ))
                    }

                    <!-- Ejemplos de Código -->
                    <section class="mb-20" data-aos="fade-up">
                        <h2
                            class="text-3xl md:text-4xl font-bold text-gray-900 mb-10"
                        >
                            Ejemplos Prácticos
                        </h2>
                        <div class="space-y-8">
                            {
                                blogPost.codeExamples.map((example) => (
                                    <div class="rounded-xl overflow-hidden border border-gray-200">
                                        <div class="p-6 bg-gray-50 border-b border-gray-200">
                                            <h3 class="font-semibold text-xl text-gray-900">
                                                {example.title}
                                            </h3>
                                            <p class="text-gray-600 mt-2">
                                                {example.description}
                                            </p>
                                        </div>
                                        <pre class="p-6 bg-gray-900 overflow-x-auto">
                                            <code class="text-gray-100 text-sm">
                                                {example.code}
                                            </code>
                                        </pre>
                                    </div>
                                ))
                            }
                        </div>
                    </section>

                    <!-- Beneficios -->
                    <section class="mb-20" data-aos="fade-up">
                        <h2
                            class="text-3xl md:text-4xl font-bold text-gray-900 mb-10"
                        >
                            Beneficios de la Integración
                        </h2>
                        <div class="grid sm:grid-cols-3 gap-8">
                            {
                                blogPost.benefits.map((benefit) => (
                                    <div
                                        class="p-8 rounded-xl bg-white border border-gray-200 
                                    hover:shadow-xl hover:shadow-blue-500/10 transition-all duration-300"
                                    >
                                        <h3 class="font-semibold text-xl text-gray-900 mb-4">
                                            {benefit.title}
                                        </h3>
                                        <p class="text-gray-600">
                                            {benefit.description}
                                        </p>
                                    </div>
                                ))
                            }
                        </div>
                    </section>
                </div>
            </div>
        </main>
    </article>
</Layout>
